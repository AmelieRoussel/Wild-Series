{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ 'episode.update'|trans }}{% endblock %}

{% block body %}
    <h1 class="text-center my-4">{{ 'episode.update'|trans }}</h1>
    <div class="container form">
        {{ include('episode/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="back my-4">
            {{ include('episode/_delete_form.html.twig') }}
        </div>

        <div class="back my-4">
            <a href="{{ path('admin_episode_index') }}">{{ 'back'|trans }}</a>
        </div>

    </div>
{% endblock %}
