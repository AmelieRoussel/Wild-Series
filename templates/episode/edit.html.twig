{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Modifier un épisode{% endblock %}

{% block body %}
    <h1 class="text-center my-4">Modifier l'épisode</h1>
    <div class="container form">
        {{ include('episode/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="back my-4">
            {{ include('episode/_delete_form.html.twig') }}
        </div>

        <div class="back my-4">
            <a href="{{ path('episode_index') }}">Retour</a>
        </div>

    </div>
{% endblock %}
