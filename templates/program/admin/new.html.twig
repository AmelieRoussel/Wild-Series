{% extends "base.html.twig" %}

{% block title %}{{ parent() }} | {{ 'program.add'|trans }}{% endblock %}

{% block body %}
    <h1 class="text-center my-4">{{ 'program.add'|trans }}</h1>
    <div class="container form">
        {{ include('program/admin/_form.html.twig') }}

        <div class="back my-4">
            <a href="{{ path('admin_program_index') }}">{{ 'back'|trans }}</a>
        </div>
    </div>
{% endblock %}