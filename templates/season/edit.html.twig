{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | {{ 'season.update'|trans }}{% endblock %}

{% block body %}
    <h1 class="text-center my-4">{{ 'season.update'|trans }}</h1>
    <div class="container form">
        {{ include('season/_form.html.twig', {'button_label': 'Modifier') }}

        <div class="back my-4">
            {{ include('season/_delete_form.html.twig') }}
        </div>

        <div class="back my-4">
            <a href="{{ path('admin_season_index') }}">{{ 'back'|trans }}</a>
        </div>

    </div>
{% endblock %}
